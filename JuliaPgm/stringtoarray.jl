# Prompt the user for a string
println("Enter a string:")
input_string = readline()

# Split the string into an array of characters
char_array = collect(input_string)

# Print the result
println("Array of characters:")
println(char_array)